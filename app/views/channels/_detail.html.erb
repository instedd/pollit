<h1>Channel</h1>
  
<div style="margin-top:-10px;margin-bottom:15px;">
  A channel created on Nuntium Local Gateway enables you to send polls from your mobile phone
</div>

<% if @poll.channel %>
  <div class="CurrentChannelBox box grey w60">
    <div class="left">
      <p class="black">Poll channel</p>
      <p class="grey">
        Bidirectional (<%=@channel.unprefixed_address %>)
        <%= ", last activity #{time_ago_in_words @poll.channel.last_activity} ago" if @channel.last_activity %>
      </p>
    </div>

    <%= link_to '', poll_channel_path(@poll, @poll.channel), :method => :delete, :class => 'icon fdelete right', :confirm => "Are you sure you want to delete this channel?" %>
  </div>
<% end %>

<div class="LocalGatewayBox box">
  <div class="LocalGatewayLogo left"></div>
  <div class="LocalGatewayText left">
    will help you to send polls from your mobile phone, 
    <%= link_to 'learn More', tour_path(1) %>.
  </div>
  <div class="LocalGatewayArrow left"></div>
  <%= grey_link_to 'Change channel', step_path(:a_choose_local_gateway), :remote => true %>
</div>
