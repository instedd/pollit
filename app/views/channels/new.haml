%h1 
  Add channel for
  = @poll.title

= form_for @channel, :method => :post, :url => { :action => "create" } do |f|
  .field.w20
    = f.label :ticket_code
    = f.text_field :ticket_code

  = f.submit "Save", :class => "grey button"
  = link_to "Cancel", @poll, :class => "white button"