- content_for :content do
  - if params[:wizard]
    - if @dotted_steps
      = dotted_wizard @dotted_steps, @wizard_step if @wizard_step
    - else
      = wizard @steps, @wizard_step
  %br
  = yield

= render :file => 'layouts/application'