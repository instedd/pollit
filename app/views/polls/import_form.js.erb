<%unless @error%>
  $('#poll_form_container').replaceWith('<%=escape_javascript(render :partial => 'form')%>');
  window.reinitialize();
  window.formValidate();
  select_field(0);
<%else%>
  window.validator.showErrors({'poll[form_url]': 'Invalid public Google Form URL'});
<%end%>